<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>twitter</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
    <body>
        <div class="width-w-full min-h-screen bg-zinc-800 text-white p-10">
            <h3>Login User</h3>
            <a href="/logout">Logout</a>
            <form action="/login" method="post">
                <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-500 rounded-md mt-2" type="email" placeholder="Email" name="email">
                <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-500 mt-2 rounded-md" type="password" placeholder="Password" name="password">
                <input class="px-5 py-2 bg-blue-500 rounded-md mt-2 text-md border-2 border-black" type="submit" value="Login">
            </form>
        </div>
    </body>
    
    
</html>