<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>twitter</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
    <body>
        <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
            <div class="w-full flex justify-end">
                <a class="bg-red-500 rounded-md px-3 py-2 text-sm" href="/logout">logout</a>

            </div>
            <h5 class="text-sm ">Edit your post</h5>
            <form action="/update/<%= post._id %>" method="post">
                <textarea placeholder="What's on your mind?" class=" block p-3 mt-1 w-1/3 resize-none bg-transparent border-2 border-zinc-500 rounded-md" name="content"><%=post.content %></textarea>
                <input class="text-sm w-40 px-3 py-2 text-black font-bold  bg-yellow-500 rounded-md block md-2 mt-2" type="submit" value="Update Post">
            </form>
             
        </div>
    </body>
</html>