<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>twitter</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
    <body>
        <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
            <div class="w-full flex justify-end">
                <a class="bg-red-500 rounded-md px-3 py-2 text-sm" href="/logout">logout</a>

            </div>
            <h3 class="text-3xl mt-2">Hello, <%= user.username %>👋</h3>
            <h5 class="text-sm ">You can create your post</h5>
            <form action="/post" method="post">
                <textarea placeholder="What's on your mind?" class=" block p-3 mt-1 w-1/3 resize-none bg-transparent border-2 border-zinc-500 rounded-md" name="content"></textarea>
                <input class="text-sm w-40 px-3 py-2 bg-blue-500 rounded-md block md-2 mt-2" type="submit" value="Create new post">
            </form>
            <div class="posts mt-20 ">
                <h3 class="text-zinc-500 ">Your Posts</h3>
                <div class="postcontainer mt-2">
                    <% user.posts.reverse().forEach(function(post){%>
                        <div class="post w-1/3 p-4 mb-2 border-2 border-zinc-500 bg-zinc-700 rounded-md">
                            <h3 class="text-blue-500 text-sm md-2">@<%=user.username%></h3>
                            <h4 class="text-sm tracking-tight"><%= post.content%></h4>
                            <small class="mt-2 inline-block"><%=post.likes.length%> like </small>
                            <div class="btns flex gap-4 mt-1">
                                <a class="text-blue-500" href="/like/<%=post._id %>">
                                    <%=post.likes.indexOf(user._id)=== -1 ? "Like 👍" : "Unlike 👎" %>
                                </a>

                                <a class="text-blue-500" href="/edit/<%=post._id %>">Edit✒️</a>
                            </div>
    
                        </div>

                    <%})%>

                    </div>
                </div>
            </div>
        </div>
    </body>
</html>